name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

permissions:
  contents: write
  packages: write
  id-token: write   # required for NuGet.org Trusted Publishing (OIDC)

jobs:
  release:
    name: Pack & Publish
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release -p:Version=${{ steps.version.outputs.VERSION }}

      - name: Run Tests
        run: |
          dotnet test tests/Mediant.UnitTests/ --no-build --configuration Release
          dotnet test tests/Mediant.IntegrationTests/ --no-build --configuration Release
          dotnet test tests/Mediant.Analyzers.Tests/ --no-build --configuration Release
          dotnet test tests/Mediant.SourceGenerator.Tests/ --no-build --configuration Release
          dotnet test tests/Mediant.EntityFrameworkCore.Tests/ --no-build --configuration Release

      - name: Pack
        run: |
          dotnet pack src/Mediant/Mediant.csproj \
            --no-build --configuration Release \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./nupkgs

          dotnet pack src/Mediant.Contracts/Mediant.Contracts.csproj \
            --no-build --configuration Release \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./nupkgs

          dotnet pack src/Mediant.Behaviors/Mediant.Behaviors.csproj \
            --no-build --configuration Release \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./nupkgs

          dotnet pack src/Mediant.FluentValidation/Mediant.FluentValidation.csproj \
            --no-build --configuration Release \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./nupkgs

          dotnet pack src/Mediant.AspNetCore/Mediant.AspNetCore.csproj \
            --no-build --configuration Release \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./nupkgs

          dotnet pack src/Mediant.Analyzers/Mediant.Analyzers.csproj \
            --no-build --configuration Release \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./nupkgs

          dotnet pack src/Mediant.SourceGenerator/Mediant.SourceGenerator.csproj \
            --no-build --configuration Release \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./nupkgs

          dotnet pack src/Mediant.EntityFrameworkCore/Mediant.EntityFrameworkCore.csproj \
            --no-build --configuration Release \
            -p:Version=${{ steps.version.outputs.VERSION }} \
            --output ./nupkgs

      # Trusted Publishing: exchanges the workflow's OIDC token for a short-lived NuGet.org
      # API key (no long-lived secret). Requires a matching trusted-publishing policy on nuget.org.
      - name: NuGet login (Trusted Publishing / OIDC)
        id: nuget-login
        uses: NuGet/login@v1
        with:
          user: omercelikdev

      - name: Push to NuGet.org
        run: |
          dotnet nuget push ./nupkgs/*.nupkg \
            --api-key ${{ steps.nuget-login.outputs.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate

      - name: Push Symbol Packages
        run: |
          dotnet nuget push ./nupkgs/*.snupkg \
            --api-key ${{ steps.nuget-login.outputs.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
        continue-on-error: true

      - name: Push to GitHub Packages
        run: |
          dotnet nuget push ./nupkgs/*.nupkg \
            --api-key ${{ secrets.GITHUB_TOKEN }} \
            --source https://nuget.pkg.github.com/omercelikdev/index.json \
            --skip-duplicate
        continue-on-error: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: ./nupkgs/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
